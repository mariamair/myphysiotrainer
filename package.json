{
  "name": "myphysiotrainer",
  "version": "1.0.0",
  "description": "MyPhysioTrainer helps users to follow the exercises and training programs that they have been given by their physiotherapist and other caregivers.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --inspect --env-file=.env src/server.js",
    "lint": "npx eslint . || exit 0",
    "lint:fix": "npx eslint . --fix || exit 0",
    "docker:dev": "docker compose -p myphysiotrainer-dev -f docker-compose.yml -f docker-compose.development.yml up -d --build",
    "docker:prod": "docker compose -p myphysiotrainer -f docker-compose.yml -f docker-compose.production.yml up -d --build",
    "test": "npx --node-options=--experimental-vm-modules jest"
  },
  "author": "Maria Mair <mm225mz@student.lnu.se>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@lnu/json-js-cycle": "^2.0.5",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-ejs-layouts": "^2.5.1",
    "express-http-context": "^2.0.0",
    "express-session": "^1.18.1",
    "helmet": "^8.1.0",
    "jest": "^29.7.0",
    "mongoose": "^8.13.2",
    "winston": "^3.17.0",
    "winston-mongodb": "^6.0.0"
  },
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.10",
    "nodemon": "^3.1.9"
  }
}
